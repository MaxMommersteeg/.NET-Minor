@model IEnumerable<Dag20.Minor.KanoWeb.Models.Kano>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.kanoID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.kanoType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.kanoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kanoType)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.kanoID }) |
                @Html.ActionLink("Details", "Details", new {  id=item.kanoID  }) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.kanoID  })
            </td>
        </tr>
}
    </tbody>
</table>
